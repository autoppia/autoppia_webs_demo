version: '3.8'

services:
  nextjs:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: web_4_autodining
    ports:
      - "8003:8003"
    volumes:
#      - .:/app
      - /app/node_modules
#    networks:
#      - app-network
    network_mode: "host"
    restart: always

#networks:
#  app-network:
#    external: true
