version: '3.8'

services:
  nextjs:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: web_5_CRM
    ports:
      - "8004:8004"
    volumes:
#      - .:/app
      - /app/node_modules
#    networks:
#      - app-network
    network_mode: "host"
    restart: always

#networks:
#  app-network:
#    external: true
