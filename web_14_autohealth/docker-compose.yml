services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        ENABLE_DYNAMIC_HTML: ${ENABLE_DYNAMIC_HTML:-false}
        ENABLE_DATA_GENERATION: ${ENABLE_DATA_GENERATION:-false}
        NEXT_PUBLIC_DATA_GENERATION: ${NEXT_PUBLIC_DATA_GENERATION:-false}
        API_URL: ${API_URL:-http://app:8080}
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-http://localhost:8090}
        ENABLE_DYNAMIC_HTML: ${ENABLE_DYNAMIC_HTML:-true}
        DYNAMIC_HTML_STRUCTURE: ${DYNAMIC_HTML_STRUCTURE:-false}
    ports:
      - "127.0.0.1:${WEB_PORT}:8013"
    restart: always
    environment:
      - API_URL=${API_URL:-http://app:8080}
      - NEXT_PUBLIC_API_URL=${NEXT_PUBLIC_API_URL:-http://localhost:8090}
      - PORT=8013
      - NODE_ENV=production
      - ENABLE_DYNAMIC_HTML=${ENABLE_DYNAMIC_HTML:-false}
      - NEXT_PUBLIC_ENABLE_DYNAMIC_HTML=${ENABLE_DYNAMIC_HTML:-false}
      - ENABLE_DATA_GENERATION=${ENABLE_DATA_GENERATION:-false}
      - NEXT_PUBLIC_DATA_GENERATION=${NEXT_PUBLIC_DATA_GENERATION:-false}
      - ENABLE_DYNAMIC_HTML=${ENABLE_DYNAMIC_HTML:-true}
      - NEXT_PUBLIC_ENABLE_DYNAMIC_HTML=${ENABLE_DYNAMIC_HTML:-true}
      - DYNAMIC_HTML_STRUCTURE=${DYNAMIC_HTML_STRUCTURE:-false}
      - NEXT_PUBLIC_DYNAMIC_HTML_STRUCTURE=${DYNAMIC_HTML_STRUCTURE:-false}
    networks:
      - apps_net

networks:
  apps_net:
    external: true
