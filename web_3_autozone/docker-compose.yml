version: '3.8'

services:
  nextjs:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: web_3_autozone
    ports:
      - "8002:8002"
    volumes:
      - .:/app
      - /app/node_modules
