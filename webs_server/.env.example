# Copy to .env and set values. Do not commit .env or put real secrets here.
HOST_UID=1000
HOST_GID=1000
WEB_PORT=8090
POSTGRES_PORT=5437
WEBS_DATA_PATH=/path/to/webs_server/data
LOG_LEVEL=info
# Optional: only needed for /datasets/generate and /datasets/generate-smart. Set via environment or GitHub Actions secrets.
# OPENAI_API_KEY=
